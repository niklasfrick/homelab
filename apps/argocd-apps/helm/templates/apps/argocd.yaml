{{- $applicationName := "argocd" }}
{{- $applicationChartName := "argocd" }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: argocd
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  sources:
  - chart: argo-cd
    repoURL: https://argoproj.github.io/argo-helm
    path: argo
    targetRevision: {{ .Values.apps.argocd.targetRevision }}
    helm:
      releaseName: argocd
      valueFiles:
      - $values/global-{{ $applicationChartName }}-helm-values.yaml
      - $values/{{ .Values.instance }}/{{ .Values.instance }}-{{ $applicationChartName }}-helm-values.yaml
  - repoURL: git@github.com:niklasfrick/homelab.git
    ref: values
    path: apps/{{ $applicationChartName }}/settings
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
