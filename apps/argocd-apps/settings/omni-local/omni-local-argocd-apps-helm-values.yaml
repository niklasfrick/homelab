instance: omni-local

projects:
  workloads:
    enabled: false

apps:
  alloy:
    targetRevision: 1.10.0 # renovate:helm depName=alloy registryUrl=https://grafana.github.io/helm-charts
    configHelmChart:
      enabled: false
  argocd:
    targetRevision: 9.5.22 # renovate:helm depName=argo-cd registryUrl=https://argoproj.github.io/argo-helm
    configHelmChart:
      enabled: false
  cert-manager:
    targetRevision: v1.20.2 # renovate:helm depName=cert-manager registryUrl=https://charts.jetstack.io
    configHelmChart:
      enabled: false
  cilium:
    targetRevision: 1.19.5 # renovate:helm depName=cilium registryUrl=https://helm.cilium.io
    configHelmChart:
      enabled: true
  # csi-driver-nfs:
  #   targetRevision: 4.13.3 # renovate:helm depName=csi-driver-nfs registryUrl=https://raw.githubusercontent.com/kubernetes-csi/csi-driver-nfs/master/charts
  #   configHelmChart:
  #     enabled: false
  external-secrets:
    targetRevision: 2.6.0 # renovate:helm depName=external-secrets registryUrl=https://charts.external-secrets.io
    configHelmChart:
      enabled: false
  gateway-api-crds:
    targetRevision: HEAD
    configHelmChart:
      enabled: false
  grafana:
    targetRevision: "5.24.0" # renovate:helm depName=grafana-operator registryUrl=https://grafana.github.io/helm-charts
    configHelmChart:
      enabled: true
  # internal-ingress-nginx:
  #   targetRevision: 4.15.1 # renovate:helm depName=ingress-nginx registryUrl=https://kubernetes.github.io/ingress-nginx
  #   configHelmChart:
  #     enabled: false
  kube-prometheus-stack:
    targetRevision: 86.3.2 # renovate:helm depName=kube-prometheus-stack registryUrl=https://prometheus-community.github.io/helm-charts
    configHelmChart:
      enabled: true
  local-path-provisioner:
    targetRevision: HEAD
    configHelmChart:
      enabled: false
  loki:
    targetRevision: 17.4.7 # renovate:helm depName=loki registryUrl=oci://ghcr.io/grafana-community/helm-charts
    configHelmChart:
      enabled: false
  # omni-bmip:
  #   targetRevision: HEAD
  #   configHelmChart:
  #     enabled: false
  omni:
    targetRevision: 2.8.2 # renovate:helm depName=omni registryUrl=oci://ghcr.io/siderolabs/charts
    configHelmChart:
      enabled: false
  tailscale-operator:
    targetRevision: 1.98.4 # renovate:helm depName=tailscale-operator registryUrl=https://pkgs.tailscale.com/helmcharts
    configHelmChart:
      enabled: true
  velero:
    targetRevision: 12.0.3 # renovate:helm depName=velero registryUrl=https://vmware-tanzu.github.io/helm-charts/
    configHelmChart:
      enabled: true
