apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: pvc-manifests-monday-0300
  namespace: velero
spec:
  schedule: "0 1 * * 1"  # IMPORTANT: Velero Pod has UTC time so CH-Time -2h
  useOwnerReferencesInBackup: true
  template:
    includedNamespaces:
    - "*"  # Include all namespaces
    excludedNamespaces:
    - "thanos"
    - "plausible"
    includedResources:
    - "*"
    excludedResources:
    - "secrets"
    snapshotVolumes: true
    storageLocation: nas-bal
    volumeSnapshotLocations:
      - rook-ceph
    ttl: 336h0m0s #14 Days retention
    defaultVolumesToRestic: false
    includeClusterResources: false
