{{- if .Values.internalGateway.enabled }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ .Values.internalGateway.name }}
  namespace: {{ .Values.internalGateway.namespace }}
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  gatewayClassName: cilium
  infrastructure:
    labels:
      cilium-lb-ip-pool: {{ .Values.internalLbIpPool.name }}
  addresses:
    - type: IPAddress
      value: {{ .Values.internalGateway.ipV4 }}
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: "{{ .Values.internalGateway.domain }}"
      allowedRoutes:
        namespaces:
          from: All
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "{{ .Values.internalGateway.domain }}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        certificateRefs:
          - kind: Secret
            name: {{ .Values.internalGateway.tlsSecretName }}
    {{- if .Values.internalGateway.omniSubdomain.enabled }}
    - name: https-omni-subdomain
      protocol: HTTPS
      port: 443
      hostname: "{{ .Values.internalGateway.omniSubdomain.domain }}"
      allowedRoutes:
        namespaces:
          from: All
        kinds:
          - group: gateway.networking.k8s.io
            kind: HTTPRoute
          - group: gateway.networking.k8s.io
            kind: GRPCRoute
      tls:
        certificateRefs:
          - kind: Secret
            name: {{ .Values.internalGateway.omniSubdomain.tlsSecretName }}
    {{- end }}
    {{- if .Values.internalGateway.workloadProxy.enabled }}
    - name: https-workload-proxy
      protocol: HTTPS
      port: 443
      hostname: "{{ .Values.internalGateway.workloadProxy.domain }}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        certificateRefs:
          - kind: Secret
            name: {{ .Values.internalGateway.workloadProxy.tlsSecretName }}
    {{- end }}
{{- end }}
{{- if .Values.externalGateway.enabled }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ .Values.externalGateway.name }}
  namespace: {{ .Values.externalGateway.namespace }}
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    {{- if .Values.externalGateway.extraAnnotations -}}
    {{ toYaml .Values.externalGateway.extraAnnotations | nindent 4 }}
    {{- end }}
spec:
  gatewayClassName: cilium
  infrastructure:
    labels:
      cilium-lb-ip-pool: {{ .Values.externalLbIpPool.name }}
  addresses:
    - type: IPAddress
      value: {{ .Values.externalGateway.ipV4 }}
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: "{{ .Values.externalGateway.domain }}"
      allowedRoutes:
        namespaces:
          from: All
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "{{ .Values.externalGateway.domain }}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        certificateRefs:
          - kind: Secret
            name: {{ .Values.externalGateway.tlsSecretName }}
{{- end }}
