loki:
  storage:
    type: s3
    bucketNames:
      chunks: omni-local/loki-chunk
    s3:
      endpoint: nas.bal.949x.net:9001
      region: bal
      secretAccessKey: ${S3_SECRET_KEY}
      accessKeyId: ${S3_ACCESS_KEY}
      s3ForcePathStyle: true
      insecure: false
